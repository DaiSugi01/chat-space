- current_user.groups.each do |group|
  = link_to group_messages_path(group) do
    %p.side-contents__groups__group__group-name 
      = group.name
    %p.side-contents__groups__group__latest-message
      メッセージはまだありません。
